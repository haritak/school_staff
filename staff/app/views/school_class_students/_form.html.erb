<%= form_with(model: school_class_student, local: true) do |form| %>
  <% if school_class_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(school_class_student.errors.count, "error") %> prohibited this school_class_student from being saved:</h2>

      <ul>
      <% school_class_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :school_student_id %>
    <%= form.text_field :school_student_id, id: :school_class_student_school_student_id %>
  </div>

  <div class="field">
    <%= form.label :school_class_id %>
    <%= form.text_field :school_class_id, id: :school_class_student_school_class_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
