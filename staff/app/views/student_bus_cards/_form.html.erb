<%= form_with(model: student_bus_card, local: true) do |form| %>
  <% if student_bus_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_bus_card.errors.count, "error") %> prohibited this student_bus_card from being saved:</h2>

      <ul>
      <% student_bus_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
