<%= form_with(model: student_personal_mobile, local: true) do |form| %>
  <% if student_personal_mobile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_personal_mobile.errors.count, "error") %> prohibited this student_personal_mobile from being saved:</h2>

      <ul>
      <% student_personal_mobile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
